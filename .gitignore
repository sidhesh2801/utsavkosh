# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# dependencies
/node_modules
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# testing
/coverage

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# env files — real keys never get committed…
.env*
# …but the template of which names to fill in does.
!.env.example

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts

# Screenshots dropped in for transcribing donor names, and the ones already
# read. Working material, not part of the app — and the inbox folder has
# already collected a saved copy of somebody's Gmail, which must not reach a
# repository on GitHub.
/reciept-ss/
/reciept-done/
